/**
 * This file is called on every configured node of your model
 */

buildscript {
    dependencies {
        classpath fileTree (dir: 'libs', includes: ['*.jar'])
    }
}

apply from: "pike.gradle"