hosts {
    localhost {
        hostname = 'testhost'
        hostgroups = 'prod'
        operatingsystem = project.operatingsystems.macosx
        environment 'jdks'
        environment 'confincludes'
    }
}








