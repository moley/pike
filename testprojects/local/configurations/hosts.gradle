hosts {
    localhost {
        hostname = 'testhost'
        hostgroups = 'prod'
        operatingsystem = project.operatingsystems.macosx
        environment 'coolproperty'
        pre 'pretask'
        post 'posttask'
    }
}








